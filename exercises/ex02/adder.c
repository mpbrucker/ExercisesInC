#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int input_to_int(char * input) {

}

int main() {
    int nums[20] = {0};
    char user_input[12];

    do {
        puts("Inter an integer: ");
        scanf("%11s", user_input);
    }  while (user_input != EOF);
}